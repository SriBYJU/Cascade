[agents]
enabled = true
max_concurrent_threads_per_session = 3
# Cascade resolves capability classes at runtime. These remain unset by default.
# default_subagent_model = "gpt-5.6-terra"
# default_subagent_reasoning_effort = "medium"

[goals]
# Feature-probed by `optimizer doctor`; kept conservative and optional.
max_goal_token_budget = 50000

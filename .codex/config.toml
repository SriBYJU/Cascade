[agents]
enabled = true
max_concurrent_threads_per_session = 3
# Cascade resolves capability classes at runtime. Concrete subagent model
# defaults remain inherited unless the user explicitly maps a capability.

[features]
# Current Codex docs expose long-running goals behind this supported flag.
# Cascade does not invent or depend on an undocumented native goal-token budget.
goals = true
